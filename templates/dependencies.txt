protobuf = "*"
serde = "*"
serde_json = "*"
serde_derive = "*"
failure = "*"
protocols = { path = "../../../protocols" }
 
[build-dependencies]
protocols = { path = "../../../protocols" }
failure = "0.1.2"

[features]
with-serde = ["protobuf/with-serde"]
default = ["with-serde"]

[lib]
crate-type = ["dylib"]
